# Day 03 – Conditional Statements

## Objective

The goal of this session is to understand how programs make decisions using conditional statements.

---

## What is a Conditional Statement?

A conditional statement allows a program to execute different blocks of code based on a condition.

Python uses:

* if
* elif
* else

---

## if Statement

The `if` statement executes a block of code only when the condition is True.

Example:

```python
age = 20

if age >= 18:
    print("Eligible to vote")
```

---

## if-else Statement

Used when there are two possible outcomes.

Example:

```python
age = 16

if age >= 18:
    print("Eligible to vote")
else:
    print("Not eligible to vote")
```

---

## if-elif-else Statement

Used when multiple conditions need to be checked.

Example:

```python
marks = 75

if marks >= 90:
    print("Grade A")
elif marks >= 75:
    print("Grade B")
else:
    print("Grade C")
```

---

## Comparison Operators

* `==` Equal to
* `!=` Not equal to
* `>` Greater than
* `<` Less than
* `>=` Greater than or equal to
* `<=` Less than or equal to

---

## Key Concepts Learned

* Decision Making
* if Statement
* if-else Statement
* if-elif-else Statement
* Comparison Operators

---

## Summary

Conditional statements help a program make decisions based on different situations and inputs.
