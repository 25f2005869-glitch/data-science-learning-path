# 📝 Day 098 — JavaScript Complete Revision Practice

**Author:** Saloni Tiwari  
**Programme:** IIT Madras BS Degree — Diploma Level  
**Day:** 098  
**Topic:** JavaScript Complete Revision

---

# Part A — Fundamentals

### Q1. What is JavaScript?

### Q2. What is the role of JavaScript in a webpage?

### Q3. What is the difference between `let`, `const`, and `var`?

### Q4. Is JavaScript case-sensitive?

### Q5. What is dynamic typing?

### Q6. What does `typeof` do?

---

# Part B — Operators

### Q7. What is the difference between `==` and `===`?

### Q8. What is the difference between `!=` and `!==`?

### Q9. Explain the `&&` operator.

### Q10. Explain the `||` operator.

### Q11. What does the `!` operator do?

### Q12. Write a ternary expression that returns `"Pass"` when marks are at least 40.

---

# Part C — Conditions

### Q13. Write an `if...else` statement to determine whether a student passed.

### Q14. Write an `else if` chain for:

    90+ → A+
    80+ → A
    70+ → B
    60+ → C
    50+ → D
    Below 50 → F

### Q15. When would you use `switch` instead of multiple conditions?

---

# Part D — Loops

### Q16. Print numbers from 1 to 10 using a `for` loop.

### Q17. Print even numbers from 2 to 20.

### Q18. Calculate the total of:

    [10, 20, 30, 40, 50]

### Q19. Explain the difference between `break` and `continue`.

### Q20. Write a `while` loop that counts from 5 down to 1.

---

# Part E — Functions

### Q21. Create a function that calculates the square of a number.

### Q22. Create a function that calculates the average of three marks.

### Q23. What is the difference between a parameter and an argument?

### Q24. What does `return` do?

### Q25. Convert a normal function into an arrow function.

---

# Part F — Strings

### Q26. Find the length of a string.

### Q27. Convert a string to uppercase.

### Q28. Remove leading and trailing spaces.

### Q29. Check whether a string contains `"JavaScript"`.

### Q30. Extract part of a string using `slice()`.

### Q31. Create a template literal containing:

    Student Name
    Course
    Marks

---

# Part G — Arrays

### Q32. Create an array containing five MAD 1 subjects.

### Q33. Add an item using `push()`.

### Q34. Remove the last item using `pop()`.

### Q35. Create a new array using `map()`.

### Q36. Filter marks greater than or equal to 50.

### Q37. Find the first student whose marks are greater than 80.

### Q38. Check whether at least one student passed using `some()`.

### Q39. Check whether every student passed using `every()`.

### Q40. Calculate the total marks using `reduce()`.

---

# Part H — Objects

### Q41. Create a student object containing:

    name
    email
    course
    marks

### Q42. Access the student's name using dot notation.

### Q43. Access the student's course using bracket notation.

### Q44. Add an `age` property.

### Q45. Update the marks.

### Q46. Delete the age property.

### Q47. Create a `greet()` method.

### Q48. Use `Object.keys()` on the student object.

---

# Part I — ES6

### Q49. What is destructuring?

### Q50. Create an array using the spread operator.

### Q51. Create a function using a rest parameter.

### Q52. Explain optional chaining.

### Q53. Explain nullish coalescing.

### Q54. What is the difference between `for...in` and `for...of`?

---

# Part J — Scope and Hoisting

### Q55. What is global scope?

### Q56. What is block scope?

### Q57. Which declaration is function-scoped: `var`, `let`, or `const`?

### Q58. What happens when `var` is accessed before its declaration?

### Q59. What is the Temporal Dead Zone?

---

# Part K — Error Handling

### Q60. Name three JavaScript error types.

### Q61. Write a `try...catch` block.

### Q62. What is the purpose of `finally`?

### Q63. What does `throw` do?

### Q64. Why should applications handle expected runtime errors?

---

# Part L — DOM

### Q65. What does DOM stand for?

### Q66. Select an element using its ID.

### Q67. Select the first element matching a CSS selector.

### Q68. Select all elements matching a CSS selector.

### Q69. Change an element's text using `textContent`.

### Q70. Add a class using `classList`.

### Q71. Create a new paragraph element.

### Q72. Remove an element from the DOM.

---

# Part M — Events

### Q73. What is an event?

### Q74. Add a click event listener to a button.

### Q75. What is `event.target`?

### Q76. What is `event.currentTarget`?

### Q77. What does `preventDefault()` do?

### Q78. What is event bubbling?

### Q79. What is event delegation?

---

# Part N — Form Validation

### Q80. What is client-side validation?

### Q81. Why is server-side validation still required?

### Q82. What does `checkValidity()` do?

### Q83. What does `reportValidity()` do?

### Q84. What is `setCustomValidity()` used for?

---

# Part O — Storage

### Q85. What is localStorage?

### Q86. What is sessionStorage?

### Q87. What is the difference between localStorage and sessionStorage?

### Q88. Store a student's name in localStorage.

### Q89. Retrieve the student's name.

### Q90. Remove the stored name.

### Q91. Store a JavaScript object using JSON.

---

# Part P — Practical Tasks

## Task 1 — Grade Calculator

Create a JavaScript program that:

- Accepts student marks.
- Calculates the grade.
- Displays pass/fail.
- Handles invalid marks.

---

## Task 2 — Student Array

Create an array of student objects.

Each student should contain:

    name
    course
    marks

Use:

    map()
    filter()
    find()
    reduce()
    some()
    every()

---

## Task 3 — DOM Manipulation

Create a page with:

- Student name
- Course
- Marks
- Button

When the button is clicked:

- Change the student's name.
- Change the course.
- Display the grade.
- Add a CSS class.

---

## Task 4 — Form Validation

Create a registration form containing:

- Name
- Email
- Password
- Course

Validate:

- Required fields
- Email
- Password length
- Course selection

---

## Task 5 — localStorage

Create a student profile and save it to localStorage.

Requirements:

- Save
- Load
- Delete
- Handle invalid JSON safely

---

# 🏆 Final JavaScript Challenge

Build a **Student Academic Dashboard** using JavaScript.

The dashboard should contain:

## Student Profile

- Name
- Email
- Course
- Marks

## Grade Calculator

Calculate:

    A+ → 90+
    A  → 80+
    B  → 70+
    C  → 60+
    D  → 50+
    F  → Below 50

## Course Manager

Support:

- Add course
- Remove course
- Search course

## Progress

Display completed course percentage.

## Validation

Validate all important user inputs.

## Storage

Use:

    localStorage
    sessionStorage

## DOM

Dynamically update:

- Student information
- Course list
- Grade
- Progress

## Error Handling

Use `try...catch` where appropriate.

---

# ✅ Final Revision Checklist

- [ ] Variables and data types
- [ ] Operators
- [ ] Conditions
- [ ] Loops
- [ ] Functions
- [ ] Strings
- [ ] Arrays
- [ ] Array methods
- [ ] Objects
- [ ] Scope
- [ ] Hoisting
- [ ] ES6
- [ ] Error handling
- [ ] DOM
- [ ] Events
- [ ] Event propagation
- [ ] Form validation
- [ ] JSON
- [ ] localStorage
- [ ] sessionStorage

---

# ⭐ Final Goal

Build the Student Academic Dashboard without copying the previous JavaScript project.

The objective is to demonstrate understanding of JavaScript from fundamentals through DOM, events, validation, and browser storage.